let num = [100,200,300,400,500];
document.write(num)
//num.splice(1,0,11)  //1st index position, number item to delete, and add the element
//num.splice(1,1,11)  //1st index position, number item to delete 1, and add the element 11 element
num.splice(1,2,11,12,13,14)  //1st index position, number item to delete 1, and add the element 11 elemen
//num.splice(1,3,11)  //1st index position, number item to delete 3, and add the element 11 element 
//num.splice(2,2);        //2nd index position, number item to delete 2
document.write("<br/>")
document.write(num)